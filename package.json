{
  "name": "majsoul-plus-client",
  "version": "1.12.0-beta.3",
  "productName": "Majsoul Plus",
  "author": "MajsoulPlus Team",
  "description": "Majsoul Plus",
  "main": "./dist/index.js",
  "scripts": {
    "sass": "node-sass manager/ -o manager/",
    "pre-build": "tsc && yarn sass && node build.js",
    "debug": "cross-env NODE_ENV=development electron ./dist --enable_logging",
    "start": "yarn pre-build && yarn debug",
    "pack": "npm run lint && npm run build",
    "build": "yarn pre-build && electron-builder --x64 --ia32",
    "build-win32": "yarn pre-build && electron-builder --win --x64 --ia32",
    "build-linux": "yarn pre-build && electron-builder --linux --x64 --ia32",
    "build-darwin": "yarn pre-build  && electron-builder --mac --x64 --ia32",
    "lint": "tslint --project .",
    "lint:fix": "tslint --project . --fix"
  },
  "build": {
    "productName": "Majsoul Plus",
    "directories": {
      "output": "build/"
    },
    "files": [
      "dist/**/*"
    ],
    "dmg": {
      "artifactName": "Majsoul_Plus-${platform}-${arch}.${ext}"
    },
    "mac": {
      "icon": "bin/icons/icon.icns",
      "asar": false,
      "artifactName": "Majsoul_Plus-${platform}-${arch}.${ext}",
      "darkModeSupport": true
    },
    "win": {
      "icon": "bin/icons/icon.ico",
      "target": [
        "nsis",
        "portable",
        "zip"
      ]
    },
    "linux": {
      "icon": "bin/icons",
      "category": "Game",
      "target": [
        "deb",
        "tar.gz",
        "AppImage"
      ]
    }
  },
  "pre-commit": [
    "lint"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iamapig120/majsoul-custorm-server.git"
  },
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/iamapig120/majsoul-custorm-server/issues"
  },
  "homepage": "https://github.com/iamapig120/majsoul-custorm-server#readme",
  "dependencies": {
    "adm-zip": "^0.4.13",
    "comma-separated-values": "^3.6.4",
    "express": "^4.16.4",
    "tcp-ping": "^0.1.1"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.4.13",
    "@types/express": "^4.16.1",
    "cross-env": "^5.2.0",
    "electron": "^4.1.3",
    "electron-builder": "^20.39.0",
    "ncp": "^2.0.0",
    "node-sass": "^4.11.0",
    "tslint": "^5.15.0",
    "typescript": "^3.4.1"
  }
}
